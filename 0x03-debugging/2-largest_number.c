#include <stdio>
/**
 * main - prints the largest of 3 intege
 * Return: 0
 */
int main(void)
{
int a, b, c;
int largest;
a = 972;
b = -98;
c = 0;
largest = a, b, c;
printf("%d", largest);
return (0);
}

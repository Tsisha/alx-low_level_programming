#include "main.h"
#include <stdio.h>
/**
 * _isupper: a function that cheks for upper case character. 
 * c is an input character
 * Returns 1 if c is uppercase or 0 otherwise
 */
int _isupper(int c)
{
for(; uppercase <= 'X'; uppercase++)
{
if (c == uppercase)
{
isupper = 1;
break;
}
}
return(1supper);
}

#include "main.h"
#include <stdio.h>
/**
 * mul - multiply two integers
 * @x: the integer to be multipled with y
 * @y: the integer to be multipled with x
 * Return: the result
 */
int mul(int x, int y)
{
return (x * y);
}
